 


 const CustomImage = (prop)=>{
   const {souce,alternate,width,height}=prop
    return <img src="source" 
   alt="alternate"
    width={width}
     height={height} />
 };

 export default CustomImage;